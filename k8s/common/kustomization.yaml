apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
- deployment.yaml
- hpa.yaml
- service.yaml

patchesStrategicMerge:
  - deployment-patch.yaml

images:
  - name: itsre/securitywiki
    newTag: latest

commonLabels:
  team: itsre
  costcenter: "1410"
